package scatter;import dataView.*;public class OutlierVariable extends NumFunctionVariable {	private NumVariable baseVariable;	private double changedValue;	private int changedIndex = -1;		public OutlierVariable(String theName, NumVariable baseVariable) {		super(theName);		this.baseVariable = baseVariable;	}		public void setOutlier(int changedIndex, double changedValue) {		this.changedIndex = changedIndex;		this.changedValue = changedValue;	}//--------------------------------------------------------		public int getMaxDecimals() {		return baseVariable.getMaxDecimals();	}		public int noOfValues() {		return baseVariable.noOfValues();	}		public Value valueAt(int index) {		NumValue baseVal = (NumValue)baseVariable.valueAt(index);				if (index == changedIndex) {			NumValue result = new NumValue(changedValue, baseVal.decimals);			return result;		}		else			return baseVal;	}}