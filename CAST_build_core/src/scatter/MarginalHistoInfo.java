package scatter;import axis.*;import histo.*;public class MarginalHistoInfo implements HistoDensityInfo {	static final private int kBorder = 4;		private int maxWidth;	private double maxDensity;		public boolean changeMaximumDensity(double maxDensity, int maxWidth) {		this.maxDensity = maxDensity;		this.maxWidth = maxWidth - kBorder;			//	to give a small border between hist & scatterplot		return true;	}		public int densityToPosition(double density) throws AxisException {		return (int)Math.round(density / maxDensity * maxWidth);	}		public double positionToDensity(int yPos) throws AxisException {		return (maxDensity * yPos) / maxWidth;	}}