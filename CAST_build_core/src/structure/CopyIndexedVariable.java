package structure;import dataView.*;public class CopyIndexedVariable extends NumFunctionVariable {	private MultiLevelDataSet data;	private NumVariable topVariable;	private int displayDecimals;		public CopyIndexedVariable(NumVariable topVariable, String varName, MultiLevelDataSet data) {		super(varName);		this.topVariable = topVariable;		this.data = data;		displayDecimals = topVariable.getMaxDecimals();	}		public CopyIndexedVariable(NumVariable topVariable, MultiLevelDataSet data) {		this(topVariable, topVariable.name, data);	}//--------------------------------------------------------		public int getMaxDecimals() {		return displayDecimals;	}		public int noOfValues() {		return data.getHigherIndex().length;	}		public Value valueAt(int index) {		int higherIndex[] = data.getHigherIndex();		int topIndex = higherIndex[index];		return topVariable.valueAt(topIndex);	}}