package random;import java.util.*;public class RandomRectangular extends RandomContinuous {	private double min, max;		public RandomRectangular(String randomInfo) {			StringTokenizer st = new StringTokenizer(randomInfo);			String countString = st.nextToken();			count = Integer.parseInt(countString);						String minString = st.nextToken();			min = Double.parseDouble(minString);						String maxString = st.nextToken();			max = Double.parseDouble(maxString);						if (st.hasMoreTokens()) {				String seedString = st.nextToken();				setSeed((Long.valueOf(seedString)).longValue());			}	}		public RandomRectangular(int count, double min, double max) {		super(count);		setMinMax(min, max);	}		public void setMinMax(double min, double max) {		this.min = min;		this.max = max;	}		protected double getQuantile(double p) {		return min + p * (max - min);	}	//	public double generateOne() {//		return min + nextDouble() * (max - min);//	}}