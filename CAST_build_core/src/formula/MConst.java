package formula;import java.awt.*;import dataView.*;public class MConst extends MFormula {	private NumValue theValue;		public MConst(NumValue theValue, FormulaContext context) {		super(context);		this.theValue = theValue;	}		protected void doInitialisation(Graphics g) {		super.doInitialisation(g);				layoutWidth = theValue.stringWidth(g);		layoutAscent = ascent;		layoutDescent = 1;//			layoutDescent = descent;	}		public void layoutContainer(Container parent) {	}		protected void paintAroundItems(Graphics g) {		int horizStart = (getSize().width - layoutWidth) / 2;		int baseline = (getSize().height + layoutAscent - layoutDescent) / 2;				theValue.drawRight(g, horizStart, baseline);	}		public void setValue(NumValue theValue) {		this.theValue = theValue;		Graphics g = getGraphics();		if (g != null) {			doInitialisation(getGraphics());			revalidate();		}	}}