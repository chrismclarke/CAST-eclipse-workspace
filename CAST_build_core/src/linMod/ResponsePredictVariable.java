package linMod;import dataView.*;import models.*;public class ResponsePredictVariable extends ResponseVariable {	static final private NumValue kNaN = new NumValue(Double.NaN);						//		Last value is used for predictions.					//		It is NaN so that it is not used for LS calculations.					//		getNewY() returns the actual new value.		public ResponsePredictVariable(String theName, DataSet data, String xKey, String errorKey,																																String modelKey) {		super(theName, data, xKey, errorKey, modelKey, 10);	}		public int noOfValues() {		return getVariable(xKey[0]).noOfValues() - 1;	}		public NumValue getNewY() {		return (NumValue)super.valueAt(noOfValues());	}		public Value valueAt(int index) {		if (index >= noOfValues())			return kNaN;		else			return super.valueAt(index);	}}