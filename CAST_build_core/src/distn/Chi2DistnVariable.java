package distn;import dataView.*;public class Chi2DistnVariable extends GammaDistnVariable {	//---------------------------------------------------------------------------------		public Chi2DistnVariable(String theName) {		super(theName);		super.setParams("2.0 2 0");		//		just to set the decimals for the shape	}		public void setParams(String s) {		String gammaParams = null;		if (s != null)			try {				NumValue shape = new NumValue(s);				shape.setValue(shape.toDouble() * 0.5);				shape.decimals = 1;								gammaParams = shape.toString() + " 2.0 0.0";			} catch (Exception e) {				System.err.println("Bad degrees of freedom for chi2 distn");			}				super.setParams(gammaParams);	}		public void setDF(int newDF) {		setShape(newDF * 0.5);		setScale(2.0);	}		public int getDF() {		return (int)Math.round(getShape().toDouble() * 2.0);	}}