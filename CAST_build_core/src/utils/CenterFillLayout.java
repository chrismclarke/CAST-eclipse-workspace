package utils;import java.awt.*;public class CenterFillLayout extends GridBagLayout {											//		fills one dimension, centres other dimension											//		fails if more than one component is added	static public final int FILL_HORIZ = 0;	static public final int FILL_VERT = 1;		int nComponents = 0;		public CenterFillLayout(int fillType) {		int gridx = 0;		int gridy = 0;		int gridwidth = 1;		int gridheight = 1;		double weightx = (fillType == FILL_HORIZ) ? 1.0 : 0.0;		double weighty = (fillType == FILL_VERT) ? 1.0 : 0.0;		int anchor = GridBagConstraints.CENTER;		int fill = (fillType == FILL_HORIZ) ? GridBagConstraints.HORIZONTAL : GridBagConstraints.VERTICAL;		Insets insets = new Insets(0,0,0,0);		int ipadx = 0;		int ipady = 0;				defaultConstraints = new GridBagConstraints(gridx, gridy, gridwidth, gridheight,                            weightx, weighty, anchor, fill, insets, ipadx, ipady);	}    public void addLayoutComponent(String name, Component comp) {  	if (nComponents > 0)  		throw new RuntimeException("Trying to add more than one component to CenterFillLayout");  	nComponents ++;  }}