package twoGroup;import dataView.*;import models.*;public class PooledSSummary extends NumSummaryVariable {	//	private String yKey, xKey;	private int decimals;		public PooledSSummary(String theName, String yKey, String xKey, int decimals) {		super(theName);		this.decimals = decimals;//		this.yKey = yKey;//		this.xKey = xKey;	}		protected NumValue evaluateSummary(DataSet sourceData) {		GroupsDataSet anovaData = (GroupsDataSet)sourceData;				int n1 = anovaData.getN(0);		double s1 = anovaData.getSD(0);		int n2 = anovaData.getN(1);		double s2 = anovaData.getSD(1);				double sPooled = Math.sqrt((s1 * s1 * (n1 - 1) + s2 * s2 * (n2 - 1)) / (n1 + n2 - 2));				return new NumValue(sPooled, decimals);	}}