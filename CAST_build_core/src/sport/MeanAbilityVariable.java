package sport;import java.util.*;import dataView.*;public class MeanAbilityVariable extends NumVariable {	private double sdIntercept, sdSlope;		public MeanAbilityVariable(String theName, String scalingParam) {		super(theName);				StringTokenizer st = new StringTokenizer(scalingParam);		sdIntercept = Double.parseDouble(st.nextToken());		sdSlope = Double.parseDouble(st.nextToken());	}	//	public double getSD(int index) {//		return getSD(valueAt(index).value);//	}		public double getSD(double ability) {		return sdIntercept + sdSlope * ability;	}		public void setPatterned(String patternString, int decimals) {		StringTokenizer st = new StringTokenizer(patternString);		double min = Double.parseDouble(st.nextToken());		double step = Double.parseDouble(st.nextToken());		int nDistinct = Integer.parseInt(st.nextToken());		int repeats = Integer.parseInt(st.nextToken());				double value[] = new double[nDistinct * repeats];				double tempVal = min;		int index = 0;		for (int i=0 ; i<nDistinct ; i++) {			for (int j=0 ;  j<repeats ; j++)				value[index++] = tempVal;			tempVal += step;		}		setValues(value);		setDecimals(decimals);	}}