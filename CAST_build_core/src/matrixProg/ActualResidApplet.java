package matrixProg;import java.awt.*;import dataView.*;import models.*;import matrix.*;public class ActualResidApplet extends GenericResidApplet {	static final protected String RESID_DECIMALS_PARAM = "residDecimals";		protected DataSet readData() {		DataSet data = super.readData();				addLsModel(data);					int residDecimals = Integer.parseInt(getParameter(RESID_DECIMALS_PARAM));			String keys[] = getXKeys();		data.addVariable("resid", new BasicComponentVariable(translate("Residual"), data, keys, "y",													"ls", BasicComponentVariable.RESIDUAL, residDecimals));				return data;	}//--------------------------------------------------------------------------------		protected ModelTerm findResidTerm(DataSet data) {		return new VariateTerm(data, "resid", true, null, 0, "e", Color.black);	}}